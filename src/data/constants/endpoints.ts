/**
 * API endpoint constants.
 *
 * Re-exports ApiEndpoints from config/urls for data-layer access.
 * Tests in this layer import from here to avoid coupling to config directly.
 */
export { ApiEndpoints } from '../../config/urls';
